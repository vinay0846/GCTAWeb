1.We nedd to create one virtual environment
2. To create requirements.txt use "pip freeze > requirements.txt"
2.we need to install the "requirements.txt" file packages using "pip install -r requirements.txt"